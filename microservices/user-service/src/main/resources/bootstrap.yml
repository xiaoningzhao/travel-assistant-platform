#spring:
#  application:
#    name: user-service
#  cloud:
#    config:
#      uri: http://localhost:8888

spring:
  application:
    name: user-service
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:127.0.0.1}:3306/tap?serverTimezone=UTC
    username: xiaoning
    password: password

server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

